## Videos

This require `vhs` to be installed to generate the videos.

In particular the `vhs` fork that supports SVG output.

```bash
brew install agentstation/tap/vhs
```

Then run the following command to generate the videos:

```bash
jbang generate.java
```

Open `output/index.html` to view the videos.